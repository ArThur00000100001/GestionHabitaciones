import { Component } from "@angular/core";
import { RouterLink } from "@angular/router";

@Component({
    selector: 'slide-view',
    templateUrl: './slide.html',
    styleUrl: './slide.scss',
    imports: [RouterLink]
})

export class slide{
    
}